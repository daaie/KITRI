프로그램 시작
메모 디렉토리 생성 (맨처음에만 생성)

1.작성
파일명 입력 (파일이름이 중복되면 안됨..새로운 이름이 입력될때까지 새로쓰게)
쓰고싶은말 계속쓰고
end입력하면 멈추고 입력받은걸 파일에 쓰게

fd=open(filename,O_CREAT|O_EXCL,"0777") ;
->O_EXCL 은 파일이 잇을때 에러발생

2. 읽기
파일명 앞에 인덱스를 붙여서 나열
오픈 보여줌

3.삭제
파일명 앞에 인덱스를 붙여서 나열
선택하면 삭제